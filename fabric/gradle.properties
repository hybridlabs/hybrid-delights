# Dependencies

fabric_kotlin_version=1.13.2

modmenu_version=7.2.2
reach_entity_attributes_version=2.4.0

# Dev Dependencies

debug_utils_version=1.0.5
carpet_version=1.4.112

# Integration Mods
# Loaded into the datagen run and the dev runs. Never compiled into or shipped with the mod.
#
# Modrinth version IDs, not version numbers.
# https://api.modrinth.com/v2/project/<slug>/version?loaders=["fabric"]&game_versions=["1.21.1"]

fantastic_fishery_version=xHpR3xVe
hybrid_aquatic_version=JbcSaVfO
hybrid_birds_version=gsEyoMhy
hybrid_blocks_version=4nUfmakP

# Hard dependencies of the above.

geckolib_version=aC5KMoNg
biolith_version=NikAXMfS
trinkets_version=AHxQGtuC
lithostitched_version=9bbVphAR

# Trinkets' own dependency, pulled from the Ladysnake maven as separate modules because the
# Modrinth bundle's nested jars are not unpacked into the dev run.

cardinal_components_version=6.1.3